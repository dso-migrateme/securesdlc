name: SecureSDLC

on:
  workflow_dispatch: {}
  pull_request: {}
  push:
    branches:
      - main
      - master

jobs:
  umbrella-include:
    uses: dso-migrateme/securesdlc-sast/.github/workflows/securesdlc-sast.yml@main
  test-step:
    steps:
      - name: Canary step
        run: |
          echo "SecureSDLC"

